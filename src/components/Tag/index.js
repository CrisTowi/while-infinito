// Libraries
import React from 'react';

// Styles
import './index.css';

const Tag = ({ title }) => {
  return (
    <div className="tag">
      <p>{title}</p>
    </div>
  );
};

export default Tag;
